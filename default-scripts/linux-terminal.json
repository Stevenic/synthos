{
    "id": "linux-terminal",
    "type": "command",
    "command": "{{command}}",
    "description": "Executes a Linux based terminal command in the current working directory",
    "variables": "{{command}}"
}